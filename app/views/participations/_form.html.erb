<%= simple_form_for(@participation) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :startup %>
    <%= f.association :event %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
