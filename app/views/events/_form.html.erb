<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label for="event_link">Event Link:</label>
    <%= f.text_field :event_link %>
  </div>
  <label for="eventlabel" class="fetched hide"></label>
  <div class="field eventid hide">
    <label for="event_id">Eventbrite ID:</label>
    <%= f.text_field :event_id %>
  </div>
  <div class="field">
    <label for="event_link">Event Name:</label>
    <%= f.text_field :event_name %>
  </div>
    <div class="field">
    <label for="event_link">Event Venue:</label>
    <%= f.text_field :event_venue %>
  </div>
  <div class="field">
    <%= f.label :country_id, "Country" %>
    <%= f.select :country_id, 
    Country.all.collect {|country| [ country.name, country.id ] }, { include_blank: false } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
