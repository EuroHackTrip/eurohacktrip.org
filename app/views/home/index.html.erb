<% provide(:title, 'Home') %>
 <div id="allmap">
   
 </div>
<div class="homewrap">
 <% if HomePageContent.all.count != 0 %>
  <%= raw HomePageContent.first.content %>
 <% end %>
 <% if admin_signed_in? && current_admin.is_admin %>
    <span class="admin">
      <%= link_to 'Edit', edit_home_page_content_path(HomePageContent.first) %>
    </span>
  <% end %>
</div>